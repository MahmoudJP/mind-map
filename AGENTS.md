# Project Instructions

- This is a public personal website.
- Edit source files under `src`; generated `index.html` and `artifact.html`
  must stay synchronized with the source.
- Keep the build process compatible with Windows and macOS.
- Never commit private exported mind-map JSON data.
- Do not deploy without explicit approval.
- Update `docs/STATUS.md` when the project state changes.

