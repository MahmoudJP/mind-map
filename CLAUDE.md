# ذاكرة المشروع — خريطة عقلي 🌳

> ملف السياق ده بيتقرا تلقائيًا في أي شات جديد مفتوح على الفولدر ده.
> فيه كل تاريخ المشروع والقرارات، عشان تكمل من غير ما تشرح من الأول.

---

## ١. إيه المشروع ده؟

موقع **خريطة ذهنية (Mind Map) شخصية** بالعربي — شكل مركزي في نص الشاشة وكل الأفكار
متفرّعة منه بفروع ملوّنة منوّرة.

**الهدف الشخصي للمستخدم:** "علشان أريّح دماغي وأشوف أفكاري كلها قدامي" — أداة راحة
نفسية وتفريغ ذهني، **مش** أداة شغل أو تعاون.

**الشكل مهم جدًا جدًا** — دي كانت أكتر نقطة اتكررت. أي شغل جاي لازم يحافظ على
مستوى بصري عالي.

## ٢. لغة التعامل

المستخدم **راجل** — كل الردود ونصوص الواجهة **بصيغة المذكر**. (اتصحّح صراحةً في
٢٠ يوليو ٢٠٢٦ بعد ما اتكتبت مؤنث بالغلط — متكررش الغلطة دي.)

المستخدم بيتكلم **مصري عامية**، والردود كلها لازم تكون بالمصري العامي.
الواجهة **بلغتين**: عربي (الافتراضي) وإنجليزي — بزرار تبديل في الشريط.

## ٣. التشغيل والملفات

```
M:\PROJECTS\mind-map\
├── index.html      ← نسخة التشغيل المحلي: doctype + charset + viewport + dir=rtl (مبنية تلقائيًا)
├── artifact.html   ← نسخة النشر كـ Artifact: بدون غلاف html/head (مبنية تلقائيًا)
├── README.md       ← دليل الاستخدام
├── CLAUDE.md       ← الملف ده
└── src\
    ├── body.html   ← الكود المصدري كله (HTML + CSS + JS) — عدّل هنا
    ├── fonts.css   ← خطوط عربية base64 مضمّنة (Tajawal + El Messiri)
    ├── server.pl   ← perl src/server.pl .  →  سيرفر اختبار على 127.0.0.1:8791
    └── build.sh    ← bash src/build.sh  →  يعيد بناء index.html + artifact.html
```

**بعد أي تعديل في `src/body.html` لازم تشغّل `bash src/build.sh`.**

الموقع منشور كمان كـ Artifact على:
`https://claude.ai/code/artifact/3bc3656e-956f-4967-8288-9fbb3863f952`
(انشر ملف `artifact.html` مع تمرير `url` بتاع الـ Artifact ده عشان اللينك ما يتغيّرش —
مينفعش تنشر `index.html` لأن فيه doctype/html/head والـ Artifacts بترفضهم)

## ٤. قرارات تصميم مهمة

- **الوضع الليلي هو الأساس** (فحمي عميق + لمسات ذهبية فخمة). فيه زرار تبديل ليلي/نهاري.
- **الخطوط:** Tajawal للنصوص، El Messiri للعناوين — **مضمّنة base64** جوه الملف،
  لأن الـ Artifacts عندها CSP بيمنع تحميل خطوط خارجية.
- **لوحة ألوان الفروع** (جواهر تلمع على الغامق):
  `#b79cff` `#4ade9e` `#56c2ff` `#ff8fb0` `#f3c775` `#45e0cf` `#8f9dff`
- **مصدر منهجية التصميم:** سكيل `ui-ux-pro-max` (نوع المنتج المطابق:
  Meditation & Mindfulness #98 + Diary/Journal #100).

## ٥. تاريخ التعديلات (ليه اتعمل كل حاجة)

| النسخة | اللي حصل |
|---|---|
| v1–v2 | نسخة غامقة "كونية" بكرة نور في النص — **المستخدم قال وحشة** |
| v3 | تحوّل لثيم فاتح هادي + شجرة — الشكل لسه مش مقنع |
| v4 | رجعنا **دارك مود فخم** بطلبه + زرار تبديل ثيم |
| v5 | إضافة مميزات احترافية (تحت) |
| v6 | **إصلاح باگ كبير:** الفروع مكانتش بتظهر خالص |
| v7 | شجرة "خطوط ذهبية" + شيل كلمة "أنا" من النص |
| v8 | الفروع تبدأ من حافة الشكل + **ميزة تغيير شكل المركز** |
| v9 | **إصلاحات + مميزات كبيرة** بعد فحص شامل بالقياس: (أ) doctype/viewport/charset — قبلها كانت quirks mode والموبايل بيرندر على 981px وهمي (ب) الواجهة بقت RTL كاملة `dir=rtl` (ج) بحث عربي مطبَّع (همزات/تاء مربوطة/تشكيل) + Enter للتنقل بين النتايج (د) نصف قطر الدايرة بقى ديناميكي `max(305, kids*27)` — صفر تداخل حتى مع 16+ فكرة (هـ) مودال تأكيد بالثيم بدل `confirm()` لأنها محجوبة في الـ Artifacts (و) أيقونات SVG موحدة بدل خليط الإيموجي (ز) منيو ⋯ + صف واحد أزرار على الموبايل (ح) مميزات جديدة: خرايط متعددة، ملاحظات جوه الأفكار، نقل فكرة لفرع تاني، وضع تركيز/تنفس، تصدير PNG، favicon، تحذير لو localStorage مش شغال |

| v10 | **صيغة المذكر + لغتين:** (أ) كل نصوص الواجهة اتحوّلت لصيغة المذكر (ب) نظام i18n كامل عربي/إنجليزي بزرار تبديل — القاموس في `I18N` والتبديل بـ `applyLang()`، الاختيار في `mm-lang`، والـ`dir` بيتظبط في سكريبت مبكر فوق خالص عشان الشكل ما يرفرفش (ج) تنقّل بالأسهم بين الأفكار `navDir()` (د) البحث بقى بيدوّر جوه الملاحظات كمان (هـ) إصلاح: `e.target.closest` كان بيرمي TypeError لما `e.target` يكون `document` نفسه |

| v11 | **١١ ميزة جديدة:** (أ) سحب فكرة وإفلاتها على فكرة تانية عشان تنقلها لفرعها (hit-test بـ`elementsFromPoint` + كلاس `drop-target`) (ب) علامة `done` مع شطب النص وعدّاد تقدّم `n/m` على الفكرة اللي ليها أبناء (ج) إيموجي على الفكرة (`icon`) بمنتقي في popover (د) روابط عرضية بين فرعين `state.xlinks` بخط منقّط (هـ) تصدير Markdown متشجّر بـcheckboxes والملاحظات كـblockquote (و) طيّ/فتح كل الفروع (ز) `createdAt` + وضع "أفكار النهاردة" (ح) زرار "رجّع" جوه التوست بعد الحذف (ط) قوالب بداية (فاضية/تفريغ ذهني/قرار/أسبوع) (ي) تذكير نسخة احتياطية بعد ٧ أيام (ك) manifest للتثبيت على الشاشة الرئيسية. وكمان: اللغة والثيم اتنقلوا للمنيو على الموبايل عشان الشريط يفضل صف واحد |

### باگّات اتحلّت (متكرّرش نفس الغلط)

1. **الكلام بيتكتب رأسي حرف تحت حرف** — السبب إن `#nodes` عرضه صفر فالكارت كان
   بينكمش لعرض حرف. الحل: `.node{width:max-content}` + `word-break:normal`.
2. **الفروع مش بتظهر خالص** — طبقة الـ SVG كانت `0×0` فمكانتش بترسم اللي بره حدودها.
   الحل: `#links` بقت `16000×16000` بإزاحة `LO=8000` على كل الإحداثيات.
3. **زرار ＋ فوق الشكل المركزي مش شغال** — رسمة الشكل كانت مغطّية الأزرار.
   الحل: `.tools{z-index:8}` + `.tree{pointer-events:none}`.
4. **الأفكار مش باينة إنها خارجة من الشكل** — كل الفروع كانت بتتلمّ في نقطة واحدة
   بالظبط. الحل: فروع المستوى الأول تبدأ من حافة الشكل (`CORE_R`) في اتجاه الفكرة.
5. **`e.target.closest is not a function`** في هاندلر الكيبورد — `e.target` ممكن يكون
   `document` نفسه (مفيهوش `closest`) فالهاندلر كله كان بيقع. الحل: `tg.closest&&tg.closest(...)`.
6. **`setPointerCapture` بيرمي `NotFoundError`** فيوقّع هاندلر `pointerdown` كله —
   يعني مفيش اختيار ولا سحب ولا تحريك خالص. الحل: `try{...}catch(_){}` حواليها.
7. **`classList.toggle(cls, val)` بيتجاهل الـ`val` لو كانت `undefined`** وبيقلب الكلاس
   بدل ما يشيله. حصل في `isFresh()` لما الجذر مكانش عنده `createdAt` فكان بيرفرف.
   الحل: أي دالة بتتبعت لـ`toggle` لازم ترجّع boolean صريح (`!!`).
8. **قياس الصفوف بـ`getBoundingClientRect().top` مضلّل** — عناصر في نفس الصف بيبقى
   ليها `top` مختلف (محاذاة رأسية)، وكمان زووم الكانفس بيصغّر كل الأرقام.
   للقياس الصح استخدم `offsetHeight`/`offsetWidth`/`offsetTop`.

## ٦. المميزات الموجودة دلوقتي

- أفكار غير محدودة، متفرّعة على أي عدد مستويات، ترتيب دائري تلقائي **بنصف قطر ديناميكي**
- **خرايط متعددة** 🗺️ (لوحة "خرايطي": إنشاء/تسمية/حذف/تنقّل)
- **ملاحظات جوه الأفكار** 📝 (زرار على كل عقدة + مؤشر صغير على الكارت)
- **نقل فكرة لفرع تاني** ⇄ (وضع نقل: زرار على العقدة وبعدين دوس على الفرع الجديد)
- **وضع تركيز/تنفس** 🧘 (الفرع المختار يفضل منوّر والباقي يعتم + أنيميشن تنفس)
- **تصدير صورة PNG** للخريطة كلها (رسم يدوي على canvas بنفس الثيم)
- **اختصارات** (زي XMind): `Enter` زميلة · `Tab` متفرّعة · `Space` تعديل · `Delete` حذف
- **تراجع/إعادة** `Ctrl+Z` / `Ctrl+Shift+Z` (تاريخ ٦٠ خطوة)
- **بحث عربي مطبَّع** `/` (همزة/تاء مربوطة/تشكيل) + `Enter`/`Shift+Enter` للتنقل بين النتايج
- سحب أي فكرة، زوّم/تحريك، طيّ وفتح الفروع، لون لكل فرع
- **تغيير شكل المركز** من مكتبة تصميمات + **رفع صورة من الجهاز**
- تصدير/استيراد JSON، وضع ليلي/نهاري، أيقونات SVG موحدة، favicon
- **لغتين عربي/إنجليزي** 🌐 بزرار تبديل — الاتجاه بيتقلب RTL/LTR ومحتوى المستخدم ما بيتترجمش
- **تنقّل بالأسهم** بين الأفكار (←→↑↓ بتختار أقرب فكرة في الاتجاه ده)
- **سحب وإفلات** فكرة على فكرة تانية عشان تنقلها لفرعها
- **علامة خلصت ✓** بشطب النص + **عدّاد تقدّم** `n/m` على الفكرة اللي ليها أبناء
- **إيموجي على الفكرة** + **روابط عرضية** بين فرعين بخط منقّط
- **تصدير Markdown** + طيّ/فتح كل الفروع + **أفكار النهاردة** + **قوالب بداية**
- **زرار رجّع جوه التوست** بعد الحذف + **تذكير نسخة احتياطية** كل ٧ أيام + **تثبيت PWA**
- واجهة RTL كاملة + منيو ⋯ للموبايل + مودال تأكيد بالثيم

## ٧. ملاحظات تقنية للي هيكمّل

- الكود كله **ملف واحد** `src/body.html` — مفيش build tools ولا مكتبات خارجية.
- العقد HTML عادية جوه `#viewport` متحوّل بـ `transform`، والفروع SVG في `#links`.
- `currentPos` = المكان المرسوم دلوقتي، `targetPos` = مكان الترتيب التلقائي.
  لو المستخدم سحب عقدة بتتخزن في `n.mx/n.my` وبتفضل ثابتة لحد ما يضغط "ترتيب".
- **تحذير:** تصميمات الشكل المركزي **من غير `<defs>` ولا `id`** — عشان الـ thumbnails
  والشكل الكبير بيتعرضوا مع بعض والـ id بيتضارب. استخدم fills/opacity مسطّحة بس.
- `state.themeVersion` بيعمل ترحيل للألوان القديمة لو غيّرنا اللوحة.
- **التخزين بعد v9:** سجل الخرايط في `mm-maps` `{list:[{id,name}],cur}`، وكل خريطة
  في `mm-map:<id>`. المفتاح القديم `mind-map-v1` بيتـرحّل تلقائيًا لـ `mm-map:m1`
  (من غير ما يتمسح — أمان). الثيم في `mm-theme`.
- **مفيش `confirm()`/`prompt()`/`alert()` خالص** — محجوبين في sandbox الـ Artifacts.
  استخدم `ask(msg,{yes,gold})` اللي بترجّع Promise<boolean> (المودال بتاع الثيم).
- التطبيع العربي للبحث في `normAr()` — لو هتعدّله اعمله على النص والاستعلام الاتنين.
- نصف قطر المستوى الأول: `max(305, عدد أفكار المستوى الأول × 27)` + `210` لكل مستوى أعمق.
- `body.html` **لازم يفضل من غير** doctype/html/head/body — غلاف الـ Artifact بيضيفهم،
  و`build.sh` بيضيف الغلاف الكامل في `index.html` بس. أي `<meta>`/`<link>` جديدة
  ضيفها بالجافاسكريبت في أول السكريبت (زي الـ favicon والـ theme-color).
- **الترجمة (v10):** كل نص واجهة جديد لازم يتحط في `I18N.ar` و `I18N.en` الاتنين،
  وفي الـ HTML استخدم `data-i18n` (نص) / `data-i18n-html` (فيه تاجات) /
  `data-i18n-ph` (placeholder) / `data-i18n-t` (title) / `data-i18n-al` (aria-label).
  في الجافاسكريبت استخدم `t("key")` — ومفيش أي نص مكتوب مباشرة في الكود.
  `applyLang()` بتعيد بناء العقد كلها عشان لابلات الأزرار تتبع اللغة.
  **محتوى المستخدم (نصوص الأفكار والملاحظات وأسماء الخرايط) عمره ما يتترجم.**
- **شكل العقدة (v11):** `{done, icon, createdAt}` جديدة، و`state.xlinks=[[a,b],...]`.
  `migrate()` بتحط الافتراضيات للخرايط القديمة وبتشيل أي رابط بيشاور على فكرة اتمسحت —
  ولازم تتنادى في `load()` و`restore()` و**الاستيراد** الاتنين.
- **`.tools` لازم يكون عليها `width:max-content`** — هي `absolute` جوه `.node` ضيّقة،
  ومن غيرها بتتحشر في عرض الكارت والأزرار تنكمش. و`.tool` عليها `flex:0 0 auto`.

## ٨. قيد مهم في بيئة Claude

الصندوق المعزول **مش بيقدر ياخد سكرين-شوت** للموقع (بيعمل timeout)، ومش بيفتح
لينك الـ Artifact (محتاج تسجيل دخول). الطريقة اللي بتشتغل للاختبار:

```bash
perl src/server.pl .        # سيرفر محلي بسيط على 127.0.0.1:8791
```
وبعدين نفتحه بـ `preview_start` ونفحص بـ `javascript_tool` (DOM + قياسات).
**النتيجة:** لازم أي تعديل بصري يتأكد **بالقياس بالكود** مش بالنظر، والمستخدم هو
اللي بيتأكد من الشكل النهائي.
